{% extends 'base.html.twig' %}
{# {% for movie in movies %}
{% block title %}{{ movie.name }}{% endblock %}
{% endfor %} #}

{% block body %}

    <main class="container mt-4">
        <div class="row">
            <div class="col-sm-5 mt-4">
                <img src="" alt="poster du film" class="w-100">
                {# {{ movie.image }} #}
            </div>
            <div class="col-sm-7">
                <div class="row mt-4 mb-2 ms-3 h1">
                    Titre
                    {# {{ movie.name }} #}
                </div>
                <div class="row mb-4 ms-3 small">
                    Réalisateur, figurants...
                    {# {{ movie.role.values }} ?? #}
                </div>
                <div class="row mb-4">
                    Lorem ipsum, dolor sit amet consectetur adipisicing elit. Magni, rerum non, voluptatum laborum inventore excepturi vitae sit dolorum iste rem quam molestias similique! Ex voluptatibus asperiores maiores saepe, illum minima quidem eligendi laboriosam ad sit molestias qui placeat veniam nihil sunt voluptatem dolor ducimus hic praesentium suscipit est assumenda ipsum.
                    {# {{ movie.description }} #}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-5 my-4">
                duration...
                {# {{ movie.duration }} #}
                cost...
                {# {{ movie.cost }} #}
                date de sortie...
                {# {{ movie.created_at }} #}
                likes...
                {# {{ movie.nb_like }} #}
                pays d'origine...
                {# {{ movie.origin_country }} #}
            </div>
        </div>
    </main>

{% endblock %}
