{% extends 'base.html.twig' %}

{% block title %}Hello SearchController!
{% endblock %}

{% block body %}
	<form>
		<div class="container card-body text-lg-center p-4 ">

			{{form_start(form)}}
			{{ form(form) }}
			{{form_end(form) }}
		</div>
	</form>
	
		{{ dump(listmovies) }}
	
{% endblock %}
